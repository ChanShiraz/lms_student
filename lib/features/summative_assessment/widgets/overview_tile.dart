import 'package:flutter/material.dart';

class OverviewTile extends StatelessWidget {
  const OverviewTile({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
